name: xwrf-dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - donfig
  - netcdf4
  - pip
  - pre-commit
  - pytest-cov
  - pytest-sugar
  - xarray>=0.18,!=0.20.0,!=0.20.1
  - pooch
  - pip:
      - -r ../requirements.txt
